local test = require("test_utils")
local object = require("object")

test.suite("object.keys", {
  test.case("simple case", function()
    --TODO:
  end),
})

test.suite("object.values", {
  test.case("simple case", function()
    --TODO:
  end),
})
